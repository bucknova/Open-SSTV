# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
.eggs/
build/
dist/
.tox/
.nox/
.cache/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
.coverage.*
coverage.xml
*.cover

# Virtual envs
.venv/
venv/
env/
ENV/

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# OS noise
.DS_Store
Thumbs.db

# Claude Code local state
.claude/

# Project-specific
# Generated test fixtures (regenerated by tests/conftest.py at session start)
tests/fixtures/wavs/
tests/fixtures/wavs_noisy/
# Decoded image output dir (when run from a checkout)
out/
*.wav.tmp
# Decoded SSTV images saved to the repo root (user decode output)
sstv_*.png
# Stale v0.2.0 snapshot sometimes left behind by tooling — never commit
Open-SSTV/
# Scratch test image at repo root (the bundled asset under src/ is unaffected
# because this pattern is anchored to the gitignore directory)
/testimage.jpg
# Design reference screenshots (working files, not part of the docs site)
docs/design/*.png
